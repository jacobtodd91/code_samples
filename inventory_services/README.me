This script does the following:

- Reads the config.json file to get instance and sql information
- Generate a token depending on whether or not an ArcGIS Enterprise contains a Portal
- Return all of the services and other information and place the data in a database